# 개인용 AI XDR 프로젝트 정리

> 다른 채팅으로 작업을 이어가기 위한 문서화 규칙은 [AGENTS.md](AGENTS.md)를 따른다.

## 1. 프로젝트 목표

그래프 엔지니어링 방식으로 여러 AI 에이전트의 실행 흐름을 구성하고, 백신 기능을 결합한 개인용 Windows XDR 프로그램을 만든다.

첫 번째 목표는 상용 XDR 전체를 재현하는 것이 아니라 다음 기능을 갖춘 최소 제품(MVP)을 만드는 것이다.

- 보안 이벤트 입력 및 정규화
- 파일·프로세스 행위·네트워크 분석
- 분석 결과 종합 및 교차 검증
- 위험 점수와 판단 근거 출력
- 대응안 제시
- 이후 프로세스 종료, 파일 격리 등 실제 대응 기능 연결

## 2. 여기서 말하는 그래프 엔지니어링

이 프로젝트에서 그래프는 두 종류로 구분한다.

### 운영 주체 비유

- 사용자: 공장 소유자이자 최종 승인 관리자
- Codex: 공장을 설계하고 코드와 규칙을 수정하는 외부 개발 엔지니어
- 실행 그래프: 작업 순서와 분기 조건이 정의된 공정 라인
- 각 노드: 정해진 역할만 수행하는 작업자 또는 작업대
- 로컬 모델: 여러 노드가 역할별 지시를 받아 사용할 수 있는 공통 추론 엔진
- YARA, Defender, Windows API: 작업자가 사용하는 검사 장비
- 공유 상태: 공정 사이를 이동하는 작업 지시서와 사건 기록
- 검증 및 정책 노드: 품질 검사와 안전 관리자

Codex가 완성된 XDR 내부에서 항상 실행되는 것은 아니다. 평상시에는 XDR 실행 그래프와 로컬 모델이 독립적으로 작동하고, 사용자가 설계 변경, 오류 수정 또는 성능 개선이 필요할 때 Codex와 협업한다.

### 실행 그래프

AI 에이전트와 일반 코드의 실행 순서를 `State`, `Node`, `Edge`로 설계한다.

```text
이벤트 입력
  → 이벤트 분류
  → 파일·행위·네트워크 병렬 분석
  → 결과 종합
  → 검증
  → 통과 또는 재분석
  → 정책 확인
  → 대응안 및 보고서 출력
```

이 실행 그래프가 AI 엔지니어링에서 말하는 그래프 엔지니어링의 핵심이다.

### 보안 지식 그래프

프로세스, 파일, 사용자, IP 등의 관계를 저장하고 공격 경로를 조사하는 데이터 구조다.

```text
WINWORD.EXE
  → PowerShell 실행
  → 실행 파일 생성
  → 외부 IP 연결
  → 시작 프로그램 등록
```

보안 지식 그래프는 실행 그래프 안의 에이전트들이 사용하는 조사 도구이자 장기 기억이다. MVP가 완성된 뒤 Neo4j나 GraphRAG 등을 검토한다.

## 3. 초기 에이전트 구성

처음에는 에이전트를 지나치게 세분화하지 않고 다음 네 역할로 시작한다.

### File Analyst

- SHA-256 계산 및 비교
- 전자서명 확인
- YARA 검사
- Microsoft Defender 검사 결과 확인

### Behavior Analyst

- 부모·자식 프로세스 분석
- 명령줄 분석
- 레지스트리 및 지속성 행위 분석
- 의심스러운 실행 흐름 탐지

### Incident Synthesizer

- 파일·행위·네트워크 결과 종합
- 공격 시나리오 구성
- 위험 점수 계산
- 대응안 제안

### Critic / Verifier

- 근거가 없는 판단 탐지
- 정상 행위일 가능성 검토
- 분석 결과 간 모순 확인
- 부족한 조사 항목 요청

에이전트끼리 단순 투표시키기보다 검증 에이전트가 증거 누락과 모순을 확인하도록 만든다. 검증 반복 횟수는 제한해 무한 루프를 방지한다.

## 4. AI와 일반 코드의 역할 구분

### 일반 코드가 담당할 작업

- 이벤트 파싱과 정규화
- 파일 해시 계산
- YARA 및 Defender 검사 실행
- 전자서명 확인
- 프로세스 트리 조회
- 점수 합산과 정책 확인
- 프로세스 종료와 파일 격리

### AI가 담당할 작업

- 여러 이벤트를 하나의 공격 흐름으로 연결
- 정상 행위와 공격 행위의 문맥 판단
- 증거 간 모순 및 누락 탐색
- 추가 조사 항목 결정
- 사용자용 사건 보고서 작성

AI가 파일 삭제나 시스템 명령을 자유롭게 실행하게 하지 않는다. AI는 구조화된 대응안을 만들고, 별도의 정책 엔진과 제한된 대응 서비스가 실제 조치를 수행한다.

## 5. 권장 기술 구성

| 용도 | 초기 선택 |
|---|---|
| 개발 언어 | Python 3.11 또는 3.12 |
| 실행 그래프 | LangGraph |
| 상태·출력 검증 | Pydantic |
| 테스트 | pytest |
| 초기 저장소 | SQLite |
| API 서버 | FastAPI(필요할 때 추가) |
| 파일 규칙 검사 | YARA(후속 단계) |
| Windows 백신 연동 | Microsoft Defender(후속 단계) |
| Windows 이벤트 수집 | Sysmon(후속 단계) |
| 지식 그래프 | Neo4j(후속 단계) |

PC에는 우선 Python과 Git 정도만 있으면 된다. 별도의 그래프 설계 프로그램은 필요하지 않다. LangGraph 라이브러리를 프로젝트의 Python 가상환경에 설치하고, 그래프를 코드로 작성한다.

```text
프로젝트 폴더/
├── .venv/          # 프로젝트 전용 Python 환경
├── src/            # XDR 소스코드
├── tests/          # 테스트
├── samples/        # 가짜 보안 이벤트
└── requirements.txt
```

## 6. AI 모델 연결 방법

LangGraph는 AI 모델이 아니라 실행 순서를 관리하는 라이브러리다. XDR의 AI 작업 노드는 실행 시 별도로 선택한 모델을 호출한다. 현재 프로젝트를 개발하는 Codex 세션이나 여러 Codex 채팅이 XDR 내부 에이전트로 자동 분리되어 들어가는 구조가 아니다.

하나의 모델을 역할별 프롬프트와 도구로 여러 번 호출하여 File Analyst, Incident Synthesizer, Critic 같은 논리적 에이전트로 구성할 수 있다. 각 역할에 서로 다른 모델을 지정하는 것도 가능하다.

모델 실행 방식은 다음 두 가지 중 하나를 선택할 수 있다.

### 클라우드 모델

- OpenAI API 등 사용
- 모델을 PC에 설치하지 않음
- API 키, 인터넷 연결 및 사용 비용 필요
- 민감한 보안 데이터의 외부 전송 범위를 통제해야 함

### 로컬 모델

- Ollama 같은 로컬 실행 도구 사용
- 데이터가 PC 밖으로 나가지 않음
- 모델 다운로드와 충분한 RAM 또는 GPU 필요

초기에는 가짜 응답 또는 규칙 기반 노드로 전체 실행 그래프를 먼저 완성하고, 이후 실제 AI 모델을 연결한다.

### 로컬 소형 모델을 사용할 수 있는 이유와 한계

그래프에서 각 노드의 입력, 역할, 도구와 출력 형식을 좁게 제한하면 작은 로컬 모델도 분류, 정보 추출, 요약처럼 한정된 작업을 안정적으로 처리할 가능성이 높아진다. 그러나 노드 분리가 모델 자체의 추론 능력을 높이는 것은 아니며, 복잡한 공격 경로 추론이나 애매한 오탐 판정은 더 강한 모델 또는 사용자 검토가 필요할 수 있다.

개인용 XDR에서는 민감한 보안 데이터의 외부 전송 방지, 반복 호출 비용 절감, 낮은 지연 시간과 오프라인 실행을 위해 로컬 모델을 사용한다. 권장 방식은 일반 코드와 보안 엔진으로 대부분의 이벤트를 처리하고, 작은 로컬 모델에는 제한된 분석을 맡기며, 판단이 어렵거나 위험한 사건만 더 강한 모델 또는 사용자에게 올리는 계층형 구성이다.

## 7. 개발 순서

### 1단계: 오프라인 분석기

- 샘플 보안 이벤트 JSON 작성
- 공통 상태 모델 구현
- LangGraph 실행 그래프 구현
- 규칙 기반 분석 노드 구현
- 위험 점수와 대응안을 JSON으로 출력

### 2단계: AI 분석 및 검증

- Incident Synthesizer에 AI 모델 연결
- Critic / Verifier 연결
- Pydantic 기반 구조화 출력 적용
- 근거 이벤트 ID와 판단 이유 저장

### 3단계: 백신 기능

- SHA-256 및 전자서명 확인
- YARA 검사
- Defender 검사 연동
- 격리 및 복원 기능 구현

### 4단계: 실제 Windows 이벤트

- Sysmon 이벤트 수집
- 프로세스 생성, 파일 생성, 네트워크 연결 이벤트 정규화
- 실시간 사건 생성 및 분석 그래프 호출

### 5단계: 지식 그래프와 장기 기억

- 프로세스·파일·IP·사건 관계 저장
- 과거 유사 사건 및 공격 경로 조회
- 필요하면 GraphRAG 적용

## 8. 첫 번째 구현 범위

첫 버전의 완료 조건은 다음과 같다.

> 샘플 보안 이벤트 JSON을 입력받아 파일·행위·네트워크 분석 결과를 종합하고, 검증 단계를 거쳐 위험 점수, 증거, 최종 판단 및 대응안을 구조화된 JSON으로 출력한다.

이 버전이 안정적으로 동작하면 샘플 입력을 Sysmon으로 교체하고, 분석 도구를 YARA와 Defender에 연결하며, 마지막으로 실제 격리 서비스를 추가한다.

## 8.1 현재 상태와 바로 다음 작업

최소 실행 그래프 골격과 모델 교체 계층을 구현했다. 현재 구현은 LangGraph와 Pydantic을 사용하며 샘플 사건을 대상으로 정규화, 파일·행위·네트워크 병렬 분석, 종합, 검증, 한 차례 재평가 및 보고서 출력까지 수행한다. 종합 노드는 `ModelAdapter` 계약을 사용하며 `RuleBasedModelAdapter`와 Ollama 기반 `qwen3:8b`를 선택할 수 있다. 실시간 수집과 시스템 대응 기능은 아직 연결하지 않았다.

완료한 내용은 다음과 같다.

1. `문서 프로그램 → PowerShell → 파일 생성 → 외부 연결` 샘플 사건 JSON
2. 사건 상태, 증거, 분석 결과, 검증 오류와 대응안을 담는 Pydantic 모델
3. 정규화, 파일·행위·네트워크 분석, 종합, 검증, 재평가와 보고서 노드
4. AI 대신 고정 규칙으로 동작하는 초기 분석기
5. 정상, 교차 확인된 의심 사건, 단일 출처 재검토 사건 테스트
6. 프로젝트 전용 `.venv`와 선언된 Python 의존성

정상·의심·증거 부족 상황을 포함한 30개 평가 사건을 추가했다. 각 사건은 기대 판정, 점수 범위와 필수 규칙을 정의한다. 전체 테스트는 `37 passed`, 기준 평가는 `30/30 passed`이며 샘플 사건은 위험도 100, `suspicious`, 검증 통과와 격리·프로세스 종료 권고를 출력한다.

로컬 환경은 Ryzen 5 7500F, RAM 31.6GB, RTX 4060 Ti 8GB이며 Ollama 0.32.6과 `qwen3:8b`를 설치했다. 모델은 약 5.8GB를 GPU에 적재한다. 30건의 정책 가드 적용 벤치마크 결과는 판정 일치율 100%, 오탐 0건, 미탐 0건, 준비 호출 3.139초, 평균 1.936초, P95 2.712초, 최대 3.005초, 규칙 모델 대체 0건이다.

초기 10건을 정책 가드 없이 호출했을 때 로컬 모델이 점수 0과 30인 두 사건을 `needs_review`로 잘못 분류해 판정 일치율이 80%였다. 이 결과에 따라 로컬 모델에는 최종 판정과 대응 권한을 주지 않는다. 운영 구조는 규칙 모델을 기본값으로 유지하고, 로컬 모델을 선택적으로 종합 노드에 사용하되 `PolicyGuardedModelAdapter`가 점수·판정·행동을 결정론적으로 재검증하며 장애 시 `FallbackModelAdapter`가 규칙 모델로 복귀하는 방식으로 확정했다.

검증 노드는 모델과 분리된 일반 코드로 유지한다. 분석 근거 존재 여부, 최소 두 출처의 교차 확인, 대응 허용 목록과 정상 판정의 무대응 원칙을 검사한다.

공통 보안 이벤트 스키마 `1.0`을 확정했다. 모든 이벤트는 스키마 버전, 이벤트 ID, 이벤트 유형, 시간대가 포함된 시각, 호스트 ID와 수집 출처를 가진다. 프로세스 생성, 파일 생성과 네트워크 연결 이벤트는 Pydantic 판별 유니온으로 분리해 유형별 필수 필드를 검사한다. 알 수 없는 필드, 잘못된 PID·포트·IP와 시간대 없는 시각은 정규화 계약 위반으로 거부한다. 전체 테스트는 `43 passed`이며 규칙 및 Ollama 실행 경로가 모두 정상 동작한다.

수집기와 분석 그래프 사이의 입력 인터페이스를 구현했다. `NormalizedEventBatch`는 배치 ID, 사건 ID, 수집기 ID, 시간대가 포함된 수신 시각과 공통 스키마 이벤트 목록을 전달한다. `EventBatchSink`가 수집기 측 입력 계약이며 `GraphIngestionService`가 현재 LangGraph 연결 구현이다. 배치 내부 이벤트 ID 중복과 잘못된 이벤트는 분석 전에 거부한다. 샘플 배치는 3개 이벤트를 처리해 위험도 100의 `suspicious` 보고서를 반환하며 전체 테스트는 `47 passed`다.

Microsoft Sysmon 15.21을 설치하고 설정 스키마 4.91 기반 최소 수집 설정을 적용했다. Sysmon64 서비스는 자동 시작 상태이고 SysmonDrv 커널 드라이버도 실행 중이다. 프로세스 생성 Event ID 1은 전체 수집하고, 네트워크 Event ID 3과 파일 생성 Event ID 11은 현재 분석 범위에 맞는 고신호 항목만 수집한다. 실제 Operational 로그에서 Event ID 1·3·11을 모두 확인했으며 전체 테스트는 `49 passed`다.

프로젝트 경로에 한글이 포함되어 Sysmon 바이너리가 설정 XML을 직접 열지 못하므로, 기준 파일은 `config/sysmon-minimal.xml`에 유지하고 실제 적용 시 `C:\ProgramData\PersonalXDR\sysmon-minimal.xml` 사본을 사용한다.

Sysmon Event ID 1·3·11 파서와 실제 이벤트 연결까지 완료했다.

작업 속도를 높이기 위해 이후 개발은 개별 체크박스 하나가 아니라 관련된 3~6개 항목을 묶어 진행한다. 세부 진행률은 기존 체크박스로 유지한다.

`W4 Sysmon 파이프라인`을 완료했다. `SysmonXmlParser`가 Event ID 1·3·11 XML을 공통 프로세스·네트워크·파일 이벤트로 변환하고, `ProcessCorrelationIndex`가 ProcessGuid를 기준으로 PID, 이미지와 프로세스 시작 시각을 후속 이벤트에 보강한다. `SysmonGraphPipeline`은 시간순 XML 묶음을 기존 배치 입력과 분석 그래프에 전달한다. 실제 설치된 Sysmon XML 3종도 모두 파싱됐으며 민감 정보가 포함될 수 있는 임시 XML은 검증 후 삭제했다. 전체 테스트는 `53 passed`다.

`W5 이벤트 저장`을 완료했다. `SQLiteEventStore`는 원본 정규화 이벤트, 배치 처리 이력과 사건별 최신 보고서를 저장한다. 전역 `event_id`로 배치 간 중복을 제거하며, 새 이벤트가 있으면 같은 사건의 기존 증거와 합쳐 그래프를 다시 실행한다. 중복만 있는 배치는 직전 보고서를 재사용해 불필요한 모델 호출을 막는다. `EventBuffer`는 이벤트 수 기준 고정 용량을 적용하고 처리 실패 시 해당 배치를 큐에 남긴다. 기본 보존 기간은 30일이며 수집 시 최대 한 시간 간격으로 오래된 이벤트·배치·사건을 자동 정리한다. 전체 테스트는 `59 passed`다.

`W6 파일 검사 엔진`을 완료했다. `FileInspectionEngine`은 파일을 고정 크기 블록으로 읽어 SHA-256, 크기, 수정 시각과 MIME 유형을 수집한다. `AuthenticodeInspector`는 코드 주입을 피하도록 대상 경로를 환경 변수로 분리해 Windows 서명 상태와 서명자를 조회한다. `YaraScanner`는 한글 프로젝트 경로에서도 동작하도록 UTF-8 규칙 본문과 파일 바이트를 직접 검사하며 기본 의심 스크립트 규칙을 제공한다. `DefenderScanner`는 사용자 지정 검사 후 해당 경로의 위협 이름을 조회하고, 권한이나 엔진 오류를 정상 결과와 구분한다. YARA, Defender와 잘못된 서명 결과는 공통 `Finding`으로 변환한다. 실제 Microsoft 서명 파일의 `valid` 상태와 관리자 권한 Defender 무해 파일 검사 결과를 확인했으며 전체 테스트는 `63 passed`다.

`W7 파일 감시 안정화`를 완료했다. 파일 검사는 기본 100MB 크기 제한과 서명·YARA·Defender별 제한 시간을 적용한다. 개별 검사 실패는 정상 결과로 숨기지 않고 부분 결과의 `errors`에 보존하며 다른 검사를 계속한다. 정상 텍스트와 실행되지 않는 의심 문자열 샘플을 이용해 안전하게 정상·탐지 경로를 검증했다. `DirectoryFileWatcher`는 다운로드와 임시 폴더의 신규 파일을 대상으로 두 번 연속 크기와 수정 시각이 같을 때만 검사하고, 심볼릭 링크·기존 파일·접근 실패가 전체 감시 루프를 중단하지 않게 한다. 전체 테스트는 `67 passed`다.

탐지 기준은 OWASP 하나에 한정하지 않는다. OWASP 최신판은 웹·애플리케이션과 AI 에이전트 위험에 적용하고, 악성코드·바이러스·웜·랜섬웨어 및 특이 행위는 MITRE ATT&CK Enterprise, Microsoft Defender, YARA와 CISA KEV를 함께 사용한다. 알려진 서명뿐 아니라 지속성, 방어 회피, 대량 파일 변경, 빠른 전파, 횡적 이동과 비정상 통신 같은 행위도 다중 증거로 상관분석한다. 세부 출처, 갱신 안전 정책과 구현 단계는 [탐지 기준 및 위협 정보 정책](docs/DETECTION_POLICY.md)을 따른다.

`W8 탐지 상관분석`을 완료했다. 기존 Python 상수 탐지 규칙을 `config/detection-rules.json`으로 분리하고 OWASP·MITRE ATT&CK·CISA 출처 버전과 참조 ID를 함께 기록한다. 후보 규칙은 Pydantic 스키마, 규칙 ID 중복, HTTPS 출처와 선택적 SHA-256을 검사한 뒤에만 활성화하며 직전 검증본으로 롤백할 수 있다. 점수·판정 임계값은 `config/risk-policy.json`으로 분리해 규칙 모델, Ollama 프롬프트와 정책 가드가 같은 기준을 사용한다. `EventCorrelationEngine`은 기본 5분 안의 이벤트를 ProcessGuid 또는 PID·시작 시각으로 연결하고 부모 ProcessGuid를 따라 프로세스 트리를 구성한다. 파일·프로세스·네트워크 원본 ID는 `AttackChain`에 보존하며 연결 근거는 점수를 중복 가산하지 않는다. 전체 테스트는 `73 passed`, 기준 평가는 `30/30 passed`다.

`W9 오탐 관리`를 완료했다. 사건 보고서에는 최종 판단에 사용된 Finding, 허용 목록으로 제외된 Finding, 공격 흐름과 정규화 원본 이벤트를 함께 저장해 규칙에서 원본 event_id까지 추적할 수 있다. 정상 행위 예외는 구체적인 호스트·프로세스·경로·SHA-256 선택자 중 하나 이상, 검토자 승인과 만료 시각이 필요하며 규칙 ID만으로 전체 탐지를 끌 수 없다. 제외된 근거도 삭제하지 않고 보고서에 남긴다. 오탐 피드백은 SQLite에 저장하며 서로 다른 사건에서 두 번 이상 승인된 동일 패턴만 허용 목록과 회귀 평가 후보가 된다. 후보 반영에는 다시 명시적 확인을 요구한다. 전체 테스트는 `80 passed`, 기준 평가는 `30/30 passed`다.

`W10 대응 기반`을 완료했다. 대응 명령은 사건 ID, 요청 시각과 대상 재식별 정보를 요구하며 프로세스 종료에는 PID·시작 시각·이미지 경로, 파일 격리에는 전체 경로·SHA-256이 필요하다. `DryRunResponseService`는 검증된 보고서의 권고, 허용 조치와 보호 프로세스·경로를 검사하지만 운영체제 명령은 호출하지 않고 항상 `executed=False`를 반환한다. 종료·격리 승인은 명령 ID에 묶이며 기본 10분 후 만료되고, 차단된 미리보기에는 승인을 만들 수 없다. 세부 안전 경계는 [보안 및 대응 정책](docs/SECURITY.md)에 기록했다. 전체 테스트는 `88 passed`, 기준 평가는 `30/30 passed`다.

다음 작업 묶음 `W11 실제 대응`은 실행 전 대상 재검증을 전제로 프로세스 종료, 파일 격리·복원, 네트워크 차단, 감사 로그와 실패 복구를 구현한다.

이 단계의 목적은 모델 성능을 평가하는 것이 아니라 공정 자체가 올바르게 연결되고, 입력과 출력 및 검증 경로가 재현 가능하게 작동하는지 확인하는 것이다.

## 9. 안전 원칙

- AI 판단만으로 파일을 영구 삭제하지 않는다.
- 자동 삭제보다 격리와 복원을 우선한다.
- Windows 핵심 파일과 시스템 프로세스는 자동 대응 대상에서 제외한다.
- 프로세스 종료 및 파일 격리 전 대상과 해시를 다시 확인한다.
- 고위험 대응에는 사용자 승인 단계를 둔다.
- 모든 분석 근거와 대응 작업을 감사 로그로 남긴다.
