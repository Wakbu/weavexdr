# 프로젝트 작업 지침

## 다른 채팅으로 이어가기 위한 문서화

- 다른 채팅이나 새로운 작업에서 알아야 할 프로젝트 맥락은 반드시 Markdown 문서에 남긴다.
- 중요한 설계, 결정, 요구사항, 구현 상태, 미해결 문제와 다음 작업을 대화에만 남겨두지 않는다.
- 의미 있는 설계 변경이나 구현 작업을 마칠 때 관련 Markdown 문서를 함께 갱신한다.
- 단순 질의응답이나 프로젝트에 영향을 주지 않는 내용은 기록하지 않아도 된다.
- 기존 문서에 들어갈 내용이면 새 문서를 만들지 말고 기존 문서를 갱신한다.
- 한 문서가 지나치게 커지거나 주제가 달라지면 목적별 문서로 분리한다.
- 문서를 분리할 때 각 문서 사이에 상대 경로 링크를 추가해 탐색할 수 있게 한다.
- 오래된 내용을 그대로 누적하지 말고, 현재 유효한 상태가 분명하게 보이도록 수정한다.
- 새 채팅에서 작업을 시작할 때 먼저 관련 Markdown 문서를 확인하고 이어서 작업한다.
- 전체 작업 순서와 완료 상태는 `PROJECT_ROADMAP.md`를 기준으로 관리한다.
- 구현과 관련 검사가 완료되면 해당 로드맵 항목을 `[x]`로 변경한다.
- 부분 완료나 검증되지 않은 작업은 완료로 체크하지 않는다.

## 권장 문서 구분

필요한 경우 다음 구조를 사용한다. 실제 내용이 생기기 전에는 빈 문서를 미리 만들지 않는다.

```text
README.md                 # 프로젝트 소개와 실행 방법
PROJECT_CONTEXT.md        # 목표, 범위, 현재 상태, 다음 작업
docs/ARCHITECTURE.md      # 시스템 및 에이전트 그래프 설계
docs/DECISIONS.md         # 중요한 결정과 선택 이유
docs/DEVELOPMENT.md       # 개발 환경, 실행, 테스트 방법
docs/SECURITY.md          # 권한, 격리, 대응 안전 정책
```

현재 초기 설계와 대화 인수인계 내용은
`XDR_AI_그래프_엔지니어링_설계_정리.md`를 기준 문서로 사용한다.
전체 개발 순서와 진행 상태는 `PROJECT_ROADMAP.md`를 기준 문서로 사용한다.

## 작성 원칙

- 다른 채팅에서 문서만 읽어도 작업 목적과 현재 상태를 이해할 수 있게 작성한다.
- 전체 대화 내용을 옮기지 말고, 확정된 내용과 후속 작업에 필요한 정보만 요약한다.
- 코드와 실제 구현이 문서보다 우선하며, 문서가 구현과 다르면 문서를 즉시 수정한다.
- 날짜별 작업 일지보다 현재 상태 중심으로 유지한다.
- API 키, 토큰, 개인정보 등 비밀 정보는 Markdown 문서에 기록하지 않는다.

## 코드 작성 규칙

- 새로 작성하거나 수정하는 코드에는 처리 의도, 중요한 분기 이유와 안전 제약을 이해할 수 있는 주석을 작성한다.
- 함수 이름만 반복 설명하는 주석보다 왜 해당 처리와 제한이 필요한지 설명한다.
- 보안 정책, 모델 대체 경로, 외부 호출, 예외 처리와 복구 로직에는 상세 주석을 우선한다.
- 변수명은 역할과 도메인이 자연스럽게 드러나도록 작성하고, 의미 없는 축약어나 기계적으로 생성된 듯한 이름을 피한다.
- 기존 코드와 명명 규칙을 유지하되 모호한 이름을 새 코드에 확산시키지 않는다.

## 작업 완료 보고

- 로드맵 항목 하나를 완료할 때마다 사용자에게 완료한 작업, 검사 결과와 다음 항목을 짧게 보고한다.
- 완료 보고 전에 `PROJECT_ROADMAP.md`의 해당 항목을 체크하고 관련 기준 문서를 갱신한다.
- 구현만 끝나고 검사가 실패했거나 남은 작업이 있으면 완료로 보고하지 않는다.
- 진행 현황은 현재 단계만이 아니라 전체 로드맵의 `완료 항목 수 / 전체 항목 수 / 완료율`로 표시한다.
- 가능하면 현재 완료된 단계, 진행 중인 단계와 바로 다음 작업도 함께 적는다.

## 작업 묶음 운영

- 세부 완료 상태는 `PROJECT_ROADMAP.md`의 개별 체크박스로 관리하되 실제 구현은 연관된 3~6개 항목을 하나의 작업 묶음으로 진행한다.
- 한 번의 작업에서는 같은 데이터 흐름이나 검증 범위에 속한 항목을 구현·통합 테스트·문서 갱신까지 함께 완료한다.
- 시스템 설치, 사용자 승인, 외부 권한처럼 독립적인 위험 경계가 있는 작업은 묶음에서 분리할 수 있다.
- 작업 묶음이 끝나면 묶음 결과와 함께 완료된 개별 체크박스 수를 전체 진행률에 반영한다.
- 다음 작업을 시작할 때 개별 항목 하나가 아니라 `PROJECT_ROADMAP.md`의 작업 묶음을 기준으로 범위를 잡는다.

## Git 커밋 및 릴리스 규칙

- 릴리스 버전 태그는 `YYYYMMDD.PATCH` 형식을 사용한다. 예: 같은 날 첫 릴리스는 `20260809.1`, 두 번째 패치는 `20260809.2`.
- 커밋과 릴리스 설명은 한국어와 영어를 함께 작성한다.
- 커밋, 태그 또는 릴리스를 원격 저장소에 올리기 전에 한글·영문·기호가 깨지지 않았는지 UTF-8 기준으로 확인한다.
- 게시 후에도 GitHub에서 제목, 본문과 변경 문서가 정상적으로 표시되는지 확인한다.
