<Sysmon schemaversion="4.91">
  <!--
    개인용 XDR 개발 단계의 최소 수집 설정이다.
    대량 이벤트보다 현재 구현된 프로세스, 파일, 네트워크 파서에 필요한
    신호를 우선하며 설정 변경 시 반드시 이벤트 발생량을 다시 측정한다.
  -->

  <!-- SHA256은 파일 식별에, IMPHASH는 PE 계열 유사성 비교에 사용한다. -->
  <HashAlgorithms>SHA256,IMPHASH</HashAlgorithms>

  <!-- 개인 PC에서 외부 인증서 조회와 역방향 DNS로 인한 지연을 줄인다. -->
  <CheckRevocation>False</CheckRevocation>
  <DnsLookup>False</DnsLookup>

  <EventFiltering>
    <!--
      Event ID 1: 모든 프로세스 생성을 기록한다.
      빈 exclude 규칙은 제외할 항목이 없다는 뜻이며 전체 이벤트가 남는다.
      부모 프로세스, 명령줄, ProcessGuid는 사건 상관분석의 핵심 입력이다.
    -->
    <ProcessCreate onmatch="exclude" />

    <!--
      Event ID 3: 전체 네트워크 연결은 소음이 크므로 공격에 자주 사용되는
      인터프리터와 사용자 쓰기 가능 경로에서 실행된 프로세스만 수집한다.
      향후 성능과 저장 용량을 측정한 뒤 범위를 확대할 수 있다.
    -->
    <NetworkConnect onmatch="include">
      <Image condition="end with">\powershell.exe</Image>
      <Image condition="end with">\pwsh.exe</Image>
      <Image condition="end with">\cmd.exe</Image>
      <Image condition="end with">\wscript.exe</Image>
      <Image condition="end with">\cscript.exe</Image>
      <Image condition="end with">\mshta.exe</Image>
      <Image condition="end with">\rundll32.exe</Image>
      <Image condition="end with">\regsvr32.exe</Image>
      <Image condition="contains">\AppData\</Image>
      <Image condition="contains">\Temp\</Image>
      <Image condition="contains">\Downloads\</Image>
    </NetworkConnect>

    <!--
      Event ID 11: 실행 파일과 스크립트 생성만 기록한다.
      문서·캐시 파일 전체를 수집하지 않아 초기 개발 중 로그 폭증을 방지한다.
    -->
    <FileCreate onmatch="include">
      <TargetFilename condition="end with">.exe</TargetFilename>
      <TargetFilename condition="end with">.dll</TargetFilename>
      <TargetFilename condition="end with">.scr</TargetFilename>
      <TargetFilename condition="end with">.ps1</TargetFilename>
      <TargetFilename condition="end with">.vbs</TargetFilename>
      <TargetFilename condition="end with">.js</TargetFilename>
      <TargetFilename condition="end with">.hta</TargetFilename>
      <TargetFilename condition="end with">.bat</TargetFilename>
      <TargetFilename condition="end with">.cmd</TargetFilename>
    </FileCreate>
  </EventFiltering>
</Sysmon>
