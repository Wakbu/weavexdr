name: release

on:
  push:
    tags:
      - "[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9].[0-9]+"

permissions:
  contents: write

jobs:
  windows-release:
    runs-on: windows-latest
    env:
      PYTHONUTF8: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m venv .venv
      # 새 가상환경의 번들 pip도 공급망 감사 대상이므로 프로젝트 설치 전에 고정 하한으로 갱신한다.
      - run: .\.venv\Scripts\python.exe -m pip install "pip>=26.1.2"
      - run: .\.venv\Scripts\python.exe -m pip install -e ".[dev]"
      - run: .\.venv\Scripts\python.exe -m pip install pip-audit
      - run: .\.venv\Scripts\python.exe -m pip_audit
      - run: .\.venv\Scripts\python.exe -m pytest -q
      - shell: powershell
        run: .\scripts\build_windows_package.ps1 -Version "${{ github.ref_name }}"
      - run: .\.venv\Scripts\python.exe scripts\validate_release.py
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/weavexdr-${{ github.ref_name }}-windows.zip
            dist/weavexdr-${{ github.ref_name }}-manifest.json
            dist/weavexdr-${{ github.ref_name }}-sbom.json
          generate_release_notes: true
